fastapi>=0.110,<1
gunicorn>=21,<24
a2wsgi>=1.7,<2

# Managed Postgres (auto-used when DATABASE_URL is set; see backend/store.py)
sqlalchemy>=2.0,<3
psycopg[binary]>=3.1,<4

# Managed Valkey/Redis cache (auto-used when REDIS_URL is set; see backend/cache.py)
redis>=5.0,<6
